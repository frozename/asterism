// Hand-curated mutants. Each entry names a source file, an exact substring to
// find and replace, and the test file(s) responsible for turning red once the
// mutation lands. A claimed test must not depend on `.git`, network, or
// anything outside the tree the runner copies -- a test that needs `.git` in
// a `.git`-less temp copy would "kill" every mutant it's handed regardless of
// the mutation, which proves nothing. That is why `test/repo-hygiene.test.mjs`
// (it shells out to git) never appears in a `claimedBy` list here.

export const MUTANTS = Object.freeze([
  Object.freeze({
    id: 'MUT-REFUSE-R1',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R1', title: "answer a permission prompt on the human's behalf, through any channel", since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R2',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R2', title: "write another program's config in place, or repair a drift it detected", since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R3',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R3', title: 'route model-authored free text into another live agent session', since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R4',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R4', title: 'auto-inject a handoff into a session the human did not explicitly resume', since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R5',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R5', title: 'run a mutating verb while inside an agent session', since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R6',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R6', title: 'write to a pane whose binding is not spawn-, agent-, or human-asserted and freshly revalidated', since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R7',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R7', title: 'answer the same prompt twice; one attempt, then escalate loudly', since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R8',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R8', title: 'pass a dangerous bypass flag, hook-disable, or hook-trust bypass to a spawned CLI', since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-R9',
    file: 'src/core/refuse.js',
    find: `  Object.freeze({ id: 'R9', title: "act on asterism's own state or binary when identity.json's sha does not match", since: 0 }),\n`,
    replace: '',
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'deleting a refuse rule must turn a test red',
  }),
  Object.freeze({
    id: 'MUT-REFUSE-NOOP',
    file: 'src/core/refuse.js',
    find: `        return { rule: 'R5', adapter: adapter.id, marker };`,
    replace: `        return null;`,
    claimedBy: Object.freeze(['test/refuse-rules.test.mjs']),
    why: 'a refusal check that can never fire is the same as no check at all',
  }),
  Object.freeze({
    id: 'MUT-PRIORITY-NOW',
    file: 'src/core/enums.js',
    find: `  High: 'high',\n});`,
    replace: `  High: 'high',\n  Now: 'now',\n});`,
    claimedBy: Object.freeze(['test/enums.test.mjs']),
    why: "an external system must not be able to preempt the user's turn",
  }),
  Object.freeze({
    id: 'MUT-VERB-REGEX',
    file: 'src/cli/router.js',
    find: `export const VERB_NAME = /^[a-z][a-z0-9-]*$/;`,
    replace: `export const VERB_NAME = /^[a-z0-9.\\/-]+$/;`,
    claimedBy: Object.freeze(['test/cli.test.mjs']),
    why: 'loosening the verb-name regex to accept "." and "/" lets a name like "../verbs/version" pass the guard and resolve through path.join to the real src/cli/verbs/version.js -- test/cli.test.mjs pins this with an "ast ../verbs/version" probe that must still exit 2 with empty stdout, which only holds if the guard rejects the traversal rather than letting it load and run a real verb',
  }),
  Object.freeze({
    id: 'MUT-SCAN-CASE',
    file: 'harness/secret-scan.mjs',
    find: `export function normalizeToken(token) {\n  return String(token).toLowerCase().replace(EDGE_PUNCTUATION, '');\n}`,
    replace: `export function normalizeToken(token) {\n  return String(token).replace(EDGE_PUNCTUATION, '');\n}`,
    claimedBy: Object.freeze(['test/secret-scan-normalizer.test.mjs']),
    why: 'a case-sensitive scanner misses a secret pasted with different capitalization',
  }),
  Object.freeze({
    id: 'MUT-REGISTRY-IGNORES-ENV',
    file: 'src/adapters/index.js',
    find: `  if (typeof env.ASTERISM_FAKE_ROOT === 'string' && env.ASTERISM_FAKE_ROOT.length > 0) {\n    registry.set(fake.id, fake);\n  }\n\n  return registry;`,
    replace: `  return registry;`,
    claimedBy: Object.freeze(['test/adapter-conformance.test.mjs']),
    why: 'a registry that ignores its env argument silently strands the conformance suite at one adapter and a seam with a single implementation rots unnoticed',
  }),
  Object.freeze({
    id: 'MUT-UNKNOWN-AT-LEAST',
    file: 'src/core/caps.js',
    find: `  return valueRank <= floorRank;`,
    replace: `  return value === UNKNOWN ? true : valueRank <= floorRank;`,
    claimedBy: Object.freeze(['test/caps.test.mjs']),
    why: 'an unknown capability must never satisfy a floor -- a feature gating on atLeast() must fail closed when the axis was never probed',
  }),
  Object.freeze({
    id: 'MUT-UNKNOWN-UNPROBED',
    file: 'src/core/caps.js',
    find: `      if (typeof evidence.probe !== 'string' || evidence.probe.length === 0) {\n        throw new Error(\`caps: axis "\${axis}" unknown evidence is missing a non-empty probe\`);\n      }\n      if (typeof evidence.deferredTo !== 'string' || evidence.deferredTo.length === 0) {\n        throw new Error(\`caps: axis "\${axis}" unknown evidence is missing deferredTo\`);\n      }`,
    replace: '',
    claimedBy: Object.freeze(['test/caps.test.mjs']),
    why: 'an unknown axis with no probe and no deferral gives doctor nothing to name -- validateRecord must reject it',
  }),
  // --- T2 mutants
  Object.freeze({
    id: 'MUT-RENDER-ESC',
    file: 'src/core/render.js',
    find: `  if (codePoint <= 0x1f) return true;`,
    replace: `  if (codePoint <= 0x1f && codePoint !== 0x1b) return true;`,
    claimedBy: Object.freeze(['test/render.test.mjs']),
    why: 'a renderer that lets ESC pass through untrusted() lets an agent-controlled name rewrite the user\'s terminal or spoof a prompt',
  }),
  Object.freeze({
    id: 'MUT-WIDTH-ZWJ',
    file: 'src/core/width.js',
    find: `export function displayWidth(str) {\n  let width = 0;\n  for (const ch of str) {\n    width += codePointWidth(ch.codePointAt(0));\n  }\n  return width;\n}`,
    replace: `export function displayWidth(str) {\n  let width = 0;\n  for (const grapheme of new Intl.Segmenter(undefined, { granularity: 'grapheme' }).segment(str)) {\n    width += codePointWidth(grapheme.segment.codePointAt(0));\n  }\n  return width;\n}`,
    claimedBy: Object.freeze(['test/width.test.mjs']),
    why: 'tmux sums a ZWJ-joined cluster\'s components rather than collapsing it -- the family emoji is width 6, a grapheme approximation says 2 -- and a grapheme-collapsing width mis-routes status-bar click ranges',
  }),
  Object.freeze({
    id: 'MUT-NAME-OWNED-FIELD',
    file: 'src/cli/pipeline.js',
    find: `export const OWNED_FIELDS = Object.freeze(['lifecycle', 'flags.parked', 'name', 'state', 'binding']);`,
    replace: `export const OWNED_FIELDS = Object.freeze(['lifecycle', 'flags.parked', 'state', 'binding']);`,
    claimedBy: Object.freeze(['test/name.test.mjs', 'test/pipeline.test.mjs']),
    why: 'collectSessions rewrites session files from reconciled records; deleting the owned name merge drops a human label on the next ast ls',
  }),
  Object.freeze({
    id: 'MUT-NAME-REFUSAL-HANDLER',
    file: 'src/cli/verbs/name.js',
    find: `  if (validation.error) return refusal(validation.error);`,
    replace: `  if (validation.error) throw new Error(validation.error);`,
    claimedBy: Object.freeze(['test/name.test.mjs']),
    why: 'name validation failures must be shaped refusal return values, not uncaught exceptions hidden by bin/ast',
  }),

  // --- T3 mutants
  Object.freeze({
    id: 'MUT-PROCSTART-STRING',
    file: 'src/core/liveness.js',
    find: `Math.abs(procStartEpoch - observedStartEpoch) <= 1`,
    replace: `String(procStartEpoch) === String(observedStartEpoch)`,
    claimedBy: Object.freeze(['test/liveness.test.mjs']),
    why: 'a string-equality liveness predicate matches zero live sessions once the registry and ps renderings disagree on timezone -- the numeric |delta|<=1 tolerance must stay numeric',
  }),
  Object.freeze({
    id: 'MUT-LIVENESS-SKIP-PROCSTART',
    file: 'src/core/liveness.js',
    find: `return pidAlive && Math.abs(procStartEpoch - observedStartEpoch) <= 1;`,
    replace: `return pidAlive;`,
    claimedBy: Object.freeze(['test/liveness.test.mjs']),
    why: 'liveness that never consults procStart cannot distinguish the live session from a pid recycled by an unrelated process',
  }),
  Object.freeze({
    id: 'MUT-FIELD-LEDGER-DROPS-PROJECTED-OMISSION',
    file: 'src/core/reconcile.js',
    find: `    if (entry.disposition === FIELD_DISPOSITIONS.PROJECTED && !projected.has(entry.key)) {`,
    replace: `    if (false) {`,
    claimedBy: Object.freeze(['test/reconcile.test.mjs']),
    why: 'a ledger audit that stops detecting a projected entry omitted from PROJECTED_FIELDS lets the two projection declarations drift silently',
  }),
  Object.freeze({
    id: 'MUT-STATUS-COLLAPSE',
    file: 'src/core/reconcile.js',
    find: `if (status === null) return 'unknown';`,
    replace: `if (status === null) return 'busy';`,
    claimedBy: Object.freeze(['test/reconcile.test.mjs']),
    why: 'a null status collapsed to "busy" invents activity a session never reported',
  }),
  Object.freeze({
    id: 'MUT-NULL-STATUS-ABSENT',
    file: 'src/core/reconcile.js',
    find: `if (value === null || (typeof value === 'string' && !KNOWN_STATUSES.includes(value))) {`,
    replace: `if (typeof value === 'string' && !KNOWN_STATUSES.includes(value)) {`,
    claimedBy: Object.freeze(['test/reconcile.test.mjs']),
    why: 'a null status is vendor data worth a canary, not a value to silently wave through unflagged',
  }),
  Object.freeze({
    id: 'MUT-UNKNOWN-KEY-THROW',
    file: 'src/core/reconcile.js',
    find: `      addCanary(canaryMap, adapter, key, fields[key]);`,
    replace: `      throw new Error('reconcile: unknown field ' + key);`,
    claimedBy: Object.freeze(['test/reconcile.test.mjs']),
    why: 'the open-world contract means an unrecognized vendor field must land a canary, never take down the whole fold',
  }),
  Object.freeze({
    id: 'MUT-ENRICH-DEFAULT',
    file: 'src/core/reconcile.js',
    find: `procStartEpoch: procStartWinner ? procStartWinner.epoch : null,`,
    replace: `procStartEpoch: procStartWinner ? procStartWinner.epoch : now,`,
    claimedBy: Object.freeze(['test/reconcile.test.mjs']),
    why: 'synthesizing procStartEpoch from now() when enrichment is absent fabricates a liveness signal the registry never supplied',
  }),
  Object.freeze({
    id: 'MUT-HEURISTIC-BOUND',
    file: 'src/core/binding.js',
    find: `return state === 'Bound' ? 'Bound' : 'Candidate';`,
    replace: `return 'Bound';`,
    claimedBy: Object.freeze(['test/binding.test.mjs']),
    why: 'a weak witness (VendorRegistry, Heuristic) must never itself promote a binding to Bound -- only a strong witness may authorize a write-eligible pane',
  }),

  // --- T4 mutants
  Object.freeze({
    id: 'MUT-STORE-SKIP-FSTAT',
    file: 'src/io/store.js',
    find: `if ((st.mode & 0o077) !== 0) {`,
    replace: `if (false) {`,
    claimedBy: Object.freeze(['test/store.test.mjs']),
    why: "a store that stops verifying the fd's mode publishes state at whatever the caller's umask allows",
  }),
  Object.freeze({
    id: 'MUT-STORE-TEMP-ELSEWHERE',
    file: 'src/io/store.js',
    find: `path.join(path.dirname(targetPath), `,
    replace: `path.join('/tmp', `,
    claimedBy: Object.freeze(['test/store.test.mjs']),
    why: 'a temp file on another filesystem makes the rename non-atomic and a crash mid-write leaves state the next start cannot parse',
  }),
  Object.freeze({
    id: 'MUT-STORE-MODE-0644',
    file: 'src/io/store.js',
    find: `{ mode = 0o600, beforeRename } = {}`,
    replace: `{ mode = 0o644, beforeRename } = {}`,
    claimedBy: Object.freeze(['test/store.test.mjs']),
    why: 'the default mode is the one every typed writer rides on',
  }),

  // --- T5 mutants
  Object.freeze({
    id: 'MUT-TMUX-DROP-U',
    file: 'src/io/tmuxexec.js',
    find: `  return execute(['tmux', '-u', '-S', socketPath, ...args, ...(command.length > 0 ? ['--', ...command] : [])], { env });`,
    replace: `  return execute(['tmux', '-S', socketPath, ...args, ...(command.length > 0 ? ['--', ...command] : [])], { env });`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'dropping -u silently re-enables the LC_ALL=C tab-mangling trap',
  }),
  Object.freeze({
    id: 'MUT-TMUX-NAME-TARGET',
    file: 'src/io/tmuxexec.js',
    find: `export const TARGET_ID = /^[$@%]\\d+$/;`,
    replace: `export const TARGET_ID = /^.+$/;`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'a name-keyed target is the wrong-pane and format-injection vector',
  }),
  Object.freeze({
    id: 'MUT-TMUX-PAD-FIELDS',
    file: 'src/core/tmuxparse.js',
    find: `    if (fields.length !== 7) {\n      return { ok: false, reason: \`list-panes row "\${line}" has \${fields.length} field(s), expected 7\` };\n    }`,
    replace: `    while (fields.length < 7) fields.push('');`,
    claimedBy: Object.freeze(['test/tmuxparse.test.mjs']),
    why: 'a padded row turns a parser failure into silent record loss',
  }),
  Object.freeze({
    id: 'MUT-TMUX-ACCEPT-EXTRA-ROWS',
    file: 'src/core/tmuxparse.js',
    find: `  if (typeof paneCount === 'number' && lines.length !== paneCount) {\n    return {\n      ok: false,\n      reason: \`list-panes returned \${lines.length} rows but the server reports \${paneCount} pane(s)\`,\n    };\n  }`,
    replace: ``,
    claimedBy: Object.freeze(['test/tmuxparse.test.mjs']),
    why: 'an embedded newline then forges extra records under -u',
  }),

  // --- T6 mutants
  Object.freeze({
    id: 'MUT-CFGEDIT-REPAIR-DRIFT',
    file: 'src/io/cfgedit.js',
    find: `  return { status: 'warn', detail: \`managed block "\${blockId}" in \${targetPath} has drifted from what asterism last wrote\` };\n`,
    replace: `  const handle = await open(targetPath, 'w');\n  await handle.writeFile(content);\n  await handle.close();\n  return { status: 'pass' };\n`,
    claimedBy: Object.freeze(['test/cfgedit.test.mjs']),
    why: 'a drift check that repairs the drift it detects is the "if missing, regenerate" failure threat T5 names, and the code half of refuse rule R2 -- report-only means report-only',
  }),
  Object.freeze({
    id: 'MUT-CFGEDIT-REMOVE-LEAVES-MARKER',
    file: 'src/io/cfgedit.js',
    find: `    const afterText = beforeText.slice(0, located.openOcc.start) + beforeText.slice(located.closeOcc.end);\n`,
    replace: `    const afterText = beforeText.slice(0, located.openOcc.end) + beforeText.slice(located.closeOcc.end);\n`,
    claimedBy: Object.freeze(['test/cfgedit.test.mjs']),
    why: 'a remove that leaves the open marker line behind fails the uninstall contract: uninstall must leave the rest of the file byte-identical to before the block ever existed',
  }),
  Object.freeze({
    id: 'MUT-CFGEDIT-NO-RESTORE',
    file: 'src/io/cfgedit.js',
    find: `    if (!verifyBuffer.equals(plan.after)) {\n      if (plan.before === null) {\n        await unlink(targetPath);\n      } else {\n        await writeViaTempAndRename(targetPath, plan.before, mode);\n      }\n      throw new Error(\`cfgedit: post-write verify failed for \${targetPath}; original bytes restored; backup at \${backupPath}\`);\n    }\n`,
    replace: '',
    claimedBy: Object.freeze(['test/cfgedit.test.mjs']),
    why: 'a byte-diff that finds a mismatch and does nothing about it turns a corrupted write into a silent success',
  }),

  // --- T7 mutants
  Object.freeze({
    id: 'MUT-ENRICH-NO-PEER-GATE',
    file: 'src/io/discover.js',
    find: `    if (parsedRecord.record.peerProtocol !== adapter.ENRICHMENT.requiredPeerProtocol) {`,
    replace: `    if (false) {`,
    claimedBy: Object.freeze(['test/discover.test.mjs']),
    why: 'an ungated registry read trusts a private file format across a protocol bump',
  }),
  Object.freeze({
    id: 'MUT-ENRICH-NO-BYTE-CAP',
    file: 'src/io/discover.js',
    find: `    if (fileStat.size > adapter.ENRICHMENT.maxFileBytes) {`,
    replace: `    if (false) {`,
    claimedBy: Object.freeze(['test/discover.test.mjs']),
    why: 'an uncapped read of an undocumented file is a memory-shaped trust grant',
  }),
  Object.freeze({
    id: 'MUT-REGISTRY-NONCANONICAL',
    file: 'src/io/discover.js',
    find: `    if (!adapter.registryFilePattern.test(name)) continue;`,
    replace: `    if (false) continue;`,
    claimedBy: Object.freeze(['test/discover.test.mjs']),
    why: 'a foreign file in the registry dir must never become a session row',
  }),

  // --- T8 mutants
  Object.freeze({
    id: 'MUT-LS-EXIT-ZERO',
    file: 'src/cli/verbs/ls.js',
    find: `  return Math.min(waiting, 125);`,
    replace: `  return 0;`,
    claimedBy: Object.freeze(['test/ls.test.mjs']),
    why: 'an exit code that stops counting waiting sessions silently blinds every scripted consumer of ast ls',
  }),
  Object.freeze({
    id: 'MUT-LS-SORT-UNSORTED',
    file: 'src/cli/verbs/ls.js',
    find: `  const sorted = [...records].sort(compareRecords);`,
    replace: `  const sorted = [...records];`,
    claimedBy: Object.freeze(['test/ls.test.mjs']),
    why: 'blocked-first is the product; an unsorted table hides the row that needs the human',
  }),
  Object.freeze({
    id: 'MUT-STORE-READ-SILENT-SKIP',
    file: 'src/io/store.js',
    find: `  errors.push(Object.freeze({ file, reason }));`,
    replace: ``,
    claimedBy: Object.freeze(['test/store.test.mjs']),
    why: 'a reader that skips a corrupt record silently turns state corruption into a session that simply vanishes from ast ls',
  }),

  // --- T9 mutants
  Object.freeze({
    id: 'MUT-HOOK-NONZERO-EXIT',
    file: 'bin/ast-hook',
    find: `process.exitCode = 0;\n`,
    replace: `process.exitCode = 1;\n`,
    claimedBy: Object.freeze(['test/ast-hook.test.mjs']),
    why: "a hook that exits non-zero blocks the human's turn -- the one thing the guest contract exists to prevent",
  }),
  Object.freeze({
    id: 'MUT-NOTIFY-NO-DEDUPE',
    file: 'src/hook/events/notification.js',
    find: `  if (entries.some((entry) => entry.key === key && eventNow - entry.at < ttlMs)) {\n`,
    replace: `  if (false) {\n`,
    claimedBy: Object.freeze(['test/notify.test.mjs']),
    why: 'without the dedupe a repeated prompt notifies on every poll and trains the human to ignore the one signal that matters',
  }),
  Object.freeze({
    id: 'MUT-HOOK-UNBOUNDED',
    file: 'src/hook/index.js',
    find: `    if (total + bytes.length > cap) {\n`,
    replace: `    if (false) {\n`,
    claimedBy: Object.freeze(['test/ast-hook.test.mjs']),
    why: "an unbounded stdin read hands a hostile payload unlimited memory inside someone else's critical path",
  }),

  // --- T10 mutants
  Object.freeze({
    id: 'MUT-IDENTITY-NO-REFUSE',
    file: 'src/io/identity.js',
    find: `  return Object.freeze({ path: identityPath, note: result.note });`,
    replace: `  return null;`,
    claimedBy: Object.freeze(['test/identity.test.mjs']),
    why: 'an installer that never refuses on a tampered manifest silently acts on a binary someone else rewrote',
  }),
  Object.freeze({
    id: 'MUT-UNINSTALL-LEAVES-MARKERS',
    file: 'src/cli/verbs/uninstall.js',
    find: `  if (!options.dryRun && tmuxPlan.action !== 'noop') {`,
    replace: `  if (false) {`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'an uninstall that leaves the managed block is how stale config accretes -- the exact fossil record the uninstall verb exists to prevent',
  }),
  Object.freeze({
    id: 'MUT-INIT-RESTARTS-BOUND-SESSIONS',
    file: 'src/cli/verbs/init.js',
    find: `  const needsRestart = [...sessions.values()].filter(({ adapter, sessionId }) =>
    !bindings.some(({ record }) =>
      record.adapter === adapter &&
      record.sessionId === sessionId &&
      STRONG_WITNESSES.includes(record.by) &&
      liveServerPids.has(record.serverPid),
    ),
  );`,
    replace: `  const needsRestart = [...sessions.values()];`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'without the usability filter init tells already-bound sessions to restart and disrupts live work for no reason',
  }),
  Object.freeze({
    id: 'MUT-INIT-RESTART-IGNORES-ADAPTER',
    file: 'src/cli/verbs/init.js',
    find: `      record.adapter === adapter &&
      record.sessionId === sessionId &&`,
    replace: `      record.sessionId === sessionId &&`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'session ids belong to adapter namespaces, so a binding from another adapter cannot make this session usable',
  }),
  Object.freeze({
    id: 'MUT-INIT-RESTART-TRUSTS-TMUX-ENV-PID',
    file: 'src/cli/verbs/init.js',
    find: `  const liveServerPids = new Set(servers.map((server) => probedPids.get(server.socketPath)).filter(Number.isInteger));`,
    replace: `  const liveServerPids = new Set(servers.map((server) => server.serverPid));`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'a stale TMUX carrier pid is not proof of the server that answered the live probe',
  }),
  Object.freeze({
    id: 'MUT-INIT-RESTART-KEYS-PROBED-ALIAS',
    file: 'src/cli/verbs/init.js',
    find: `      if (result.ok === true) probedPids.set(socketPath, result.pid);`,
    replace: `      if (result.ok === true) probedPids.set(result.socketPath, result.pid);`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'the resolver returns the canonical candidate path, so a probe-reported alias cannot key the accepted server pid',
  }),
  Object.freeze({
    id: 'MUT-INIT-REFRESH-WRITES-CONFIG',
    file: 'src/cli/verbs/init.js',
    find: `  if (options.refresh) {
    return refreshIdentity({ dryRun: options.dryRun, root: ctx.root, identityPath, env });
  }`,
    replace: `  if (options.refresh) {
    await writeTextAtomic(configPath, DEFAULT_CONFIG_TOML, { mode: 0o600 });
    return refreshIdentity({ dryRun: options.dryRun, root: ctx.root, identityPath, env });
  }`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'refresh is a narrow re-attestation and must not rewrite configuration as though it were a full install',
  }),
  Object.freeze({
    id: 'MUT-INIT-REFRESH-SILENT',
    file: 'src/cli/verbs/init.js',
    find: `    process.stdout.write(\`init refresh: \${dryRun ? 'would re-attest' : 're-attested'} \${relativePath} (sha256 moved)\\n\`);`,
    replace: ``,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'a silent refresh turns explicit human re-attestation into an unreviewable rubber stamp',
  }),
  Object.freeze({
    id: 'MUT-INIT-REFRESH-CREATES-MANIFEST',
    file: 'src/cli/verbs/init.js',
    find: `  if (existingIdentity === null) return refreshRefusal('identity manifest is absent');`,
    replace: `  if (false) return refreshRefusal('identity manifest is absent');`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'refresh must refuse without an existing install manifest instead of crossing the install boundary or crashing',
  }),
  Object.freeze({
    id: 'MUT-INIT-REFRESH-ACCEPTS-BAD-DIGEST',
    file: 'src/cli/verbs/init.js',
    find: `    !Array.isArray(value.files) &&
    Object.values(value.files).every((digest) => typeof digest === 'string' && SHA256_HEX.test(digest));`,
    replace: `    !Array.isArray(value.files);`,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'a non-SHA value is a malformed manifest, not a tree change the refresh path may silently bless',
  }),
  Object.freeze({
    id: 'MUT-INIT-REFRESH-SKIPS-STORE-GUARD',
    file: 'src/cli/verbs/init.js',
    find: `    try {
      await openStore({ env });
    } catch (error) {
      process.stderr.write(\`init refresh: \${error?.message ?? error}\\n\`);
      return 1;
    }`,
    replace: ``,
    claimedBy: Object.freeze(['test/init-uninstall.test.mjs']),
    why: 'identity must not be rewritten until the existing store passes its schema and owner-only directory guards',
  }),

  // --- T11 mutants
  Object.freeze({
    id: 'MUT-GO-CLIENT-SESSION-NAME',
    file: 'src/cli/verbs/go.js',
    find: `  const outcome = await switchClient({ clientName: chosen.clientName, target: paneId, socketPath, env });`,
    replace: `  const outcome = await switchClient({ clientName: chosen.clientName, target: listed.rows[0].sessionId, socketPath, env });`,
    claimedBy: Object.freeze(['test/go.test.mjs']),
    why: 'the client row is metadata for -c, never a target; a neighbouring client-session field is a name and ids-only targets keep it out of -t',
  }),
  Object.freeze({
    id: 'MUT-BIND-ACCEPT-NAME',
    file: 'src/cli/verbs/bind.js',
    find: `  if (!PANE_ID.test(paneId)) return refusal(\`pane id "\${paneId}" does not match ^%\\\\d+$\`);`,
    replace: `  if (false) return refusal(\`pane id "\${paneId}" does not match ^%\\\\d+$\`);`,
    claimedBy: Object.freeze(['test/bind.test.mjs']),
    why: 'a name-shaped pane id accepted here becomes a name-keyed tmux target one layer down, violating the ids-only invariant',
  }),

  // --- T12 mutants
  Object.freeze({
    id: 'MUT-DOCTOR-PERMS-IGNORE-GROUP-BIT',
    file: 'src/io/store.js',
    find: `      if ((info.mode & 0o077) !== 0) {\n`,
    replace: `      if ((info.mode & 0o007) !== 0) {\n`,
    claimedBy: Object.freeze(['test/doctor.test.mjs']),
    why: 'a permissions audit that ignores the group bits certifies a state file every group member can read',
  }),
  Object.freeze({
    id: 'MUT-DOCTOR-UNKNOWN-IS-GREEN',
    file: 'src/doctor/index.js',
    find: `  const exit = results.every((result) => result.status === 'pass' || result.status === 'warn') ? 0 : 1;\n`,
    replace: `  const exit = results.every((result) => result.status === 'pass' || result.status === 'warn' || result.status === 'unknown') ? 0 : 1;\n`,
    claimedBy: Object.freeze(['test/doctor.test.mjs']),
    why: 'a check that cannot run is not a passing check -- collapsing unknown into green certifies a broken install',
  }),
  Object.freeze({
    id: 'MUT-DOCTOR-VERB-COUNTS-DROP-UNKNOWN',
    file: 'src/cli/verbs/doctor.js',
    find: `  const counts = { pass: 0, warn: 0, fail: 0, todo: 0, unknown: 0 };\n`,
    replace: `  const counts = { pass: 0, warn: 0, fail: 0, todo: 0 };\n`,
    claimedBy: Object.freeze(['test/doctor.test.mjs']),
    why: 'an unknown result outside the counts object renders the summary as NaN and vanishes from the one line a human reads',
  }),

  // --- T13 mutants
  Object.freeze({
    id: 'MUT-LIFECYCLE-PARK-IDEMPOTENT',
    file: 'src/core/lifecycle.js',
    find: `  Parked: Object.freeze({ unpark: 'Live', archive: 'Archived' }),`,
    replace: `  Parked: Object.freeze({ park: 'Parked', unpark: 'Live', archive: 'Archived' }),`,
    claimedBy: Object.freeze(['test/lifecycle.test.mjs']),
    why: 'silently accepting a repeated park discards the refusal that tells the human the session was already parked',
  }),
  Object.freeze({
    id: 'MUT-LIFECYCLE-ARCHIVED-NOT-ABSORBING',
    file: 'src/core/lifecycle.js',
    find: `  Archived: Object.freeze({}),`,
    replace: `  Archived: Object.freeze({ unpark: 'Live' }),`,
    claimedBy: Object.freeze(['test/lifecycle.test.mjs']),
    why: 'an archived session must stay absorbing while Phase 2 exposes no unarchive control',
  }),
  Object.freeze({
    id: 'MUT-LIFECYCLE-UNKNOWN-STATE-DEFAULT',
    file: 'src/core/lifecycle.js',
    find: `  if (!Object.hasOwn(TABLE, state)) {\n    throw new LifecycleVocabularyError(\`lifecycle: unknown state "\${state}"\`);\n  }`,
    replace: `  if (!Object.hasOwn(TABLE, state)) {\n    return;\n  }`,
    claimedBy: Object.freeze(['test/lifecycle.test.mjs']),
    why: 'defaulting an unknown state turns a vocabulary defect into an unrelated result or runtime error',
  }),
  Object.freeze({
    id: 'MUT-LIFECYCLE-UNKNOWN-EVENT-BARE-TYPEERROR',
    file: 'src/core/lifecycle.js',
    find: `    throw new LifecycleVocabularyError(\`lifecycle: unknown event "\${event}"\`);`,
    replace: `    throw new TypeError(\`lifecycle: unknown event "\${event}"\`);`,
    claimedBy: Object.freeze(['test/lifecycle.test.mjs']),
    why: 'a bare TypeError erases the structural distinction between unknown lifecycle vocabulary and other type failures',
  }),

  // --- T18 mutants
  Object.freeze({
    id: 'MUT-ARCHIVE-REMOVE-FIRST',
    file: 'src/io/store.js',
    find: `      await writeTextAtomic(archivePath, bytes);\n      if (beforeRemove) await beforeRemove(archivePath);\n      if ((await readFile(archivePath, 'utf8')) !== bytes) {\n        throw new Error(\`archiveSession: archive verification failed for \${ulid}\`);\n      }\n      await unlink(sourcePath);`,
    replace: `      await unlink(sourcePath);\n      await writeTextAtomic(archivePath, bytes);\n      if (beforeRemove) await beforeRemove(archivePath);\n      if ((await readFile(archivePath, 'utf8')) !== bytes) {\n        throw new Error(\`archiveSession: archive verification failed for \${ulid}\`);\n      }`,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'removing the source before the verified archive copy can lose the only readable record on a crash',
  }),
  Object.freeze({
    id: 'MUT-ARCHIVE-SKIP-VERIFY',
    file: 'src/io/store.js',
    find: `      if ((await readFile(archivePath, 'utf8')) !== bytes) {\n        throw new Error(\`archiveSession: archive verification failed for \${ulid}\`);\n      }`,
    replace: ``,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'source removal must be gated on reading back the exact archive bytes that were written',
  }),
  Object.freeze({
    id: 'MUT-ARCHIVE-DROP-ARCHIVE-LOOKUP',
    file: 'src/cli/verbs/archive.js',
    find: `  if (resolved.error?.startsWith('no session matches ')) {\n    const archive = await readArchive(store.stateDir);\n    const archived = resolveSessionRef(archive.records.map((entry) => entry.record), argv[0]);\n    if (archived.record !== undefined) resolved = archived;\n  }`,
    replace: ``,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'an archived lookup is required to report the absorbing lifecycle refusal instead of pretending the record never existed',
  }),
  Object.freeze({
    id: 'MUT-ARCHIVE-UNKNOWN-THROWS',
    file: 'src/cli/verbs/archive.js',
    find: `  if (resolved.error) return refusal(resolved.error);`,
    replace: `  if (resolved.error) throw new Error(resolved.error);`,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'an unknown ref is a shaped refusal return value, not an uncaught exception rendered similarly by bin/ast',
  }),
  Object.freeze({
    id: 'MUT-ARCHIVE-ILLEGAL-THROWS',
    file: 'src/cli/verbs/archive.js',
    find: `    return refusal(\`\${state}: \${error instanceof Error ? error.message : String(error)}\`);`,
    replace: `    throw error;`,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'an illegal archive transition must resolve to a refusal code rather than reject from run()',
  }),
  Object.freeze({
    id: 'MUT-ARCHIVE-UNKNOWN-LIFECYCLE-THROWS',
    file: 'src/cli/verbs/archive.js',
    find: `    if (error instanceof LifecycleVocabularyError) {\n      return refusal(\`\${resolved.record.agent.sessionId}: \${state}: \${error.message}\`);\n    }`,
    replace: `    if (error instanceof LifecycleVocabularyError) {\n      throw error;\n    }`,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'a corrupt stored lifecycle value must resolve to an actionable refusal naming the session, not a stack trace',
  }),
  Object.freeze({
    id: 'MUT-ARCHIVE-BYPASS-FSM',
    file: 'src/cli/verbs/archive.js',
    find: `    lifecycle = transition(state, 'archive');`,
    replace: `    lifecycle = 'Archived';`,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'hard-coding Archived bypasses the absorbing-state refusal enforced by the lifecycle transition table',
  }),
  Object.freeze({
    id: 'MUT-LS-REJECT-ALL',
    file: 'src/cli/verbs/ls.js',
    find: `      options.all = true;`,
    replace: `      return null;`,
    claimedBy: Object.freeze(['test/ls.test.mjs', 'test/archive.test.mjs']),
    why: 'the --all parser branch is the public route to archived rows',
  }),
  Object.freeze({
    id: 'MUT-LS-SHOW-ARCHIVE-BY-DEFAULT',
    file: 'src/cli/verbs/ls.js',
    find: `    const archived = options.all ? (await readArchive(store.stateDir)).records.map((entry) => entry.record) : [];`,
    replace: `    const archived = (await readArchive(store.stateDir)).records.map((entry) => entry.record);`,
    claimedBy: Object.freeze(['test/archive.test.mjs']),
    why: 'archived history must stay hidden from the default live-session listing',
  }),
  Object.freeze({
    id: 'MUT-LS-DROP-ARCHIVED-MARKER',
    file: 'src/cli/verbs/ls.js',
    find: `    record.lifecycle === 'Archived' ? 'archived' : statusLabel(record.observed.status),`,
    replace: `    statusLabel(record.observed.status),`,
    claimedBy: Object.freeze(['test/ls.test.mjs', 'test/archive.test.mjs']),
    why: 'an archived row rendered with its stale observed status appears live',
  }),
  Object.freeze({
    id: 'MUT-LS-COUNT-ARCHIVED-WAITING',
    file: 'src/cli/verbs/ls.js',
    find: `  return record.lifecycle !== 'Archived' && record.observed.status === 'waiting';`,
    replace: `  return record.observed.status === 'waiting';`,
    claimedBy: Object.freeze(['test/ls.test.mjs', 'test/archive.test.mjs']),
    why: 'archived waiting state is historical and must not count as current operator attention',
  }),
  Object.freeze({
    id: 'MUT-RECONCILE-MINT-WITHOUT-PAIR',
    file: 'src/core/reconcile.js',
    find: `    const id = mint(group.adapter, group.sessionId);`,
    replace: `    const id = mint();`,
    claimedBy: Object.freeze(['test/pipeline.test.mjs']),
    why: 'the pipeline must see each group identity before minting so an archive tombstone can reuse its existing id',
  }),
  Object.freeze({
    id: 'MUT-PIPELINE-MINT-ARCHIVED-ANOTHER-ID',
    file: 'src/cli/pipeline.js',
    find: `    mint: (adapter, sessionId) => archivedBySession.get(sessionKey(adapter, sessionId)) ?? mintActive(),`,
    replace: `    mint: () => mintActive(),`,
    claimedBy: Object.freeze(['test/pipeline.test.mjs']),
    why: 'a still-live archived session must not consume a new ULID before suppression',
  }),
  Object.freeze({
    id: 'MUT-PIPELINE-REMATERIALISE-ARCHIVED',
    file: 'src/cli/pipeline.js',
    find: `  const active = reconciled.records.filter(\n    (record) => !archivedBySession.has(sessionKey(record.adapter, record.agent.sessionId)),\n  );`,
    replace: `  const active = reconciled.records;`,
    claimedBy: Object.freeze(['test/pipeline.test.mjs']),
    why: 'a vendor observation matching an archive tombstone must be removed before the live write loop',
  }),
  Object.freeze({
    id: 'MUT-PIPELINE-DROPS-ARCHIVE-UNREADABLE-NOTE',
    file: 'src/cli/pipeline.js',
    find: `  for (const error of archive.errors) {\n    notes.push(note('store', 'archive-unreadable', \`\${error.file}: \${error.reason}\`));\n  }`,
    replace: ``,
    claimedBy: Object.freeze(['test/pipeline.test.mjs']),
    why: 'unreadable archived records are real degradation and must surface as operator notes',
  }),
  // --- T14 mutants
  Object.freeze({
    id: 'MUT-NEWWINDOW-NO-DETACH',
    file: 'src/io/tmuxexec.js',
    find: `    ['new-window', ...(detached ? ['-d'] : []), '-P', '-F', NEW_WINDOW_FORMAT, '-c', cwd],`,
    replace: `    ['new-window', '-P', '-F', NEW_WINDOW_FORMAT, '-c', cwd],`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'a default spawn that omits detached mode moves the human tmux view as a side effect',
  }),
  Object.freeze({
    id: 'MUT-NEWWINDOW-RELATIVE-CWD',
    file: 'src/io/tmuxexec.js',
    find: `  if (typeof cwd !== 'string' || !path.isAbsolute(cwd)) {`,
    replace: `  if (typeof cwd !== 'string' || false) {`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'a relative cwd resolves against the tmux server rather than the caller',
  }),
  Object.freeze({
    id: 'MUT-NEWWINDOW-FORMAT-UNSAFE-CWD',
    file: 'src/io/tmuxexec.js',
    find: `  assertFormatSafe(cwd);\n`,
    replace: ``,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'a format-expanded cwd can turn path bytes into tmux format output',
  }),
  Object.freeze({
    id: 'MUT-NEWWINDOW-RAW-STDOUT',
    file: 'src/io/tmuxexec.js',
    find: `  const paneId = result.stdout.toString('utf8').trim();\n  if (!TARGET_ID.test(paneId) || !paneId.startsWith('%')) {\n    throw new Error(\`tmux new-window returned invalid pane id \${JSON.stringify(paneId)}\`);\n  }\n  return paneId;`,
    replace: `  return result.stdout.toString('utf8');`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'returning raw tmux output lets an unvalidated or untrimmed target reach persistent state',
  }),
  Object.freeze({
    id: 'MUT-SPAWNARGV-ACCEPTS-ULID',
    file: 'src/core/uuid.js',
    find: `export const UUID_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/;\n\nfunction hex(byte) {`,
    replace: `export const UUID_PATTERN = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|[0-9A-HJKMNP-TV-Z]{26})$/;\n\nfunction hex(byte) {`,
    claimedBy: Object.freeze(['test/adapter-conformance.test.mjs', 'test/uuid.test.mjs']),
    why: 'the vendor launch flag requires a UUID; accepting the primary store id shape conflates two identities',
  }),
  Object.freeze({
    id: 'MUT-UUID-ACCEPTS-ANY-VERSION',
    file: 'src/core/uuid.js',
    find: `export const UUID_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/;\n\nfunction hex(byte) {\n  return byte.toString(16).padStart(2, '0');\n}`,
    replace: `export const UUID_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-[0-9a-f]{4}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/;\n\nfunction hex(byte) {\n  return byte.toString(16).padStart(2, '0');\n}`,
    claimedBy: Object.freeze(['test/uuid.test.mjs']),
    why: 'dropping the version nibble silently re-admits non-v4 UUIDs at every adapter boundary',
  }),
  Object.freeze({
    id: 'MUT-SPAWNARGV-UNFROZEN',
    file: 'src/adapters/claude/spawn.js', // quarantine-exempt: real adapter path this mutant patches.
    find: `  return Object.freeze(['claude', '--session-id', sessionId]);`, // quarantine-exempt: real adapter argv string this mutant patches.
    replace: `  return ['claude', '--session-id', sessionId];`, // quarantine-exempt: real adapter argv string this mutant patches.
    claimedBy: Object.freeze(['test/adapter-conformance.test.mjs']),
    why: 'a mutable argv can be altered after validation and before execution',
  }),

  // --- T15 mutants
  Object.freeze({
    id: 'MUT-NEWWINDOW-COMMAND-JOIN',
    file: 'src/io/tmuxexec.js',
    find: `    { socketPath, env, execute, command },`,
    replace: `    { socketPath, env, execute, command: [command.join(' ')] },`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'joining the agent argv destroys argument boundaries and turns a validated argv vector into one command-like string',
  }),
  Object.freeze({
    id: 'MUT-NEWWINDOW-COMMAND-NONARRAY',
    file: 'src/io/tmuxexec.js',
    find: `  if (!Array.isArray(command)) throw new Error('new-window command argv must be an array');`,
    replace: `  if (false) throw new Error('new-window command argv must be an array');`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'a command string is iterable but is not an argv vector and must be refused before tmux is invoked',
  }),
  Object.freeze({
    id: 'MUT-NEWWINDOW-COMMAND-ELEMENTS',
    file: 'src/io/tmuxexec.js',
    find: `    if (typeof element !== 'string' || /[\\n\\r\\x00]/.test(element)) {`,
    replace: `    if (false) {`,
    claimedBy: Object.freeze(['test/tmuxexec.test.mjs']),
    why: 'non-string, NUL, and newline command elements must be refused before tmux is invoked',
  }),
  Object.freeze({
    id: 'MUT-NEW-WRITE-AFTER-SPAWN',
    file: 'src/cli/verbs/new.js',
    find: `  await store.writeSession(id, record);\n\n  const command = selected.adapter.spawnArgv({ sessionId });\n  const paneId = await newWindow({\n    cwd: options.cwd,\n    command,\n    detached: !options.switchWindow,\n    socketPath: server.socketPath,\n    env: ctx.env,\n    execute: ctx.execute,\n  });`,
    replace: `  const command = selected.adapter.spawnArgv({ sessionId });\n  const paneId = await newWindow({\n    cwd: options.cwd,\n    command,\n    detached: !options.switchWindow,\n    socketPath: server.socketPath,\n    env: ctx.env,\n    execute: ctx.execute,\n  });\n  await store.writeSession(id, record);`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'the Unbound row must be durable before the process can start so a crash-on-start remains visible',
  }),
  Object.freeze({
    id: 'MUT-NEW-WEAK-WITNESS',
    file: 'src/cli/verbs/new.js',
    find: `  await store.writeBinding(mintUlid(), {\n    sessionId,\n    adapter: selected.adapter.id,\n    by: 'SpawnMinted',`,
    replace: `  await store.writeBinding(mintUlid(), {\n    sessionId,\n    adapter: selected.adapter.id,\n    by: 'Heuristic',`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'a process launched directly by asterism has a spawn-minted witness, not a weak inferred binding',
  }),
  Object.freeze({
    id: 'MUT-NEW-REUSE-SESSION-ID-FOR-BINDING',
    file: 'src/cli/verbs/new.js',
    find: `  await store.writeBinding(mintUlid(), {`,
    replace: `  await store.writeBinding(id, {`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'bindings are independent spool artifacts and require a freshly minted binding ULID',
  }),
  Object.freeze({
    id: 'MUT-NEW-MULTI-ADAPTER-FIRST',
    file: 'src/cli/verbs/new.js',
    find: `  if (entries.length === 1) return { adapter: entries[0][1] };`,
    replace: `  if (entries.length > 0) return { adapter: entries[0][1] };`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'silently selecting the first adapter makes launch identity depend on registry insertion order',
  }),
  Object.freeze({
    id: 'MUT-NEW-NO-SERVER-CONTINUE',
    file: 'src/cli/verbs/new.js',
    find: `  if (servers.length === 0) return refusal('ast new: no tmux server is reachable');`,
    replace: `  if (false) return refusal('ast new: no tmux server is reachable');`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'continuing without a reachable server creates an orphan Unbound row before spawn fails',
  }),
  Object.freeze({
    id: 'MUT-NEW-IGNORE-SWITCH',
    file: 'src/cli/verbs/new.js',
    find: `    detached: !options.switchWindow,`,
    replace: `    detached: true,`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: '--switch is the explicit control that permits the new window to become current',
  }),
  Object.freeze({
    id: 'MUT-NEW-ALWAYS-PRINT',
    file: 'src/cli/verbs/new.js',
    find: `  if (!options.switchWindow) process.stdout.write(\`\${id} \${paneId}\\n\`);`,
    replace: `  if (true) process.stdout.write(\`\${id} \${paneId}\\n\`);`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'the detached receipt is not emitted when --switch already exposes the new window to the client',
  }),
  Object.freeze({
    id: 'MUT-NEW-DUPLICATE-SWITCH',
    file: 'src/cli/verbs/new.js',
    find: `      if (switchWindow) return null;`,
    replace: `      if (false) return null;`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'accepting --switch twice weakens the one-flag CLI grammar and hides caller mistakes',
  }),
  Object.freeze({
    id: 'MUT-NEW-EXTRA-POSITIONAL',
    file: 'src/cli/verbs/new.js',
    find: `      if (arg.startsWith('--') || cwd !== null) return null;`,
    replace: `      if (false) return null;`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'new accepts at most one cwd and refuses unknown flags instead of reinterpreting them as paths',
  }),
  Object.freeze({
    id: 'MUT-RESOLVE-SERVERS-DROP-NONENOENT',
    file: 'src/io/tmuxsock.js',
    find: `    addNote(notes, 'socket-canonicalization-failed', socketPath, error);\n    return { socketPath, canonical: false };`,
    replace: `    addNote(notes, 'socket-canonicalization-failed', socketPath, error);\n    return null;`,
    claimedBy: Object.freeze(['test/go.test.mjs']),
    why: 'a realpath failure other than ENOENT does not prove that a tmux socket is dead, so the authoritative probe must still run',
  }),
  Object.freeze({
    id: 'MUT-RESOLVE-SERVERS-DROP-NOTE',
    file: 'src/io/tmuxsock.js',
    find: `  notes.push(Object.freeze({ adapter: 'tmux', note: name, detail: \`\${socketPath}: \${errorMessage(error)}\` }));`,
    replace: `  return;`,
    claimedBy: Object.freeze(['test/go.test.mjs']),
    why: 'non-ENOENT socket resolution failures must remain visible through the note channel',
  }),
  Object.freeze({
    id: 'MUT-RESOLVE-SERVERS-RECOVER-ENOENT',
    file: 'src/io/tmuxsock.js',
    find: `    if (error?.code === 'ENOENT') return null;`,
    replace: `    if (false) return null;`,
    claimedBy: Object.freeze(['test/go.test.mjs']),
    why: 'ENOENT means the candidate disappeared and must not be probed or invented as a live server',
  }),
  Object.freeze({
    id: 'MUT-RESOLVE-SERVERS-SILENT-PROBE-ERROR',
    file: 'src/io/tmuxsock.js',
    find: `      if (error?.code !== 'ENOENT') addNote(notes, 'socket-probe-failed', candidate.socketPath, error);`,
    replace: `      if (false) addNote(notes, 'socket-probe-failed', candidate.socketPath, error);`,
    claimedBy: Object.freeze(['test/go.test.mjs']),
    why: 'a probe exception other than ENOENT is degradation, not an ordinary negative, and must produce a note',
  }),
  Object.freeze({
    id: 'MUT-RESOLVE-SERVERS-DEDUPE-PATH-ONLY',
    file: 'src/io/tmuxsock.js',
    find: `    const identity = Number.isInteger(probed.pid) ? \`pid:\${probed.pid}\` : \`socket:\${socketPath}\`;`,
    replace: `    const identity = \`socket:\${socketPath}\`;`,
    claimedBy: Object.freeze(['test/go.test.mjs']),
    why: 'the probed server pid deduplicates aliases even when socket canonicalisation is unavailable',
  }),
  Object.freeze({
    id: 'MUT-NEW-USAGE-THROWS',
    file: 'src/cli/verbs/new.js',
    find: `    process.stderr.write(USAGE);\n    return 2;`,
    replace: `    process.stderr.write(USAGE);\n    throw new Error('usage refusal crashed');`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'malformed argv is a refusal value returned by run, not an exception',
  }),
  Object.freeze({
    id: 'MUT-NEW-USAGE-GUARD-DELETED',
    file: 'src/cli/verbs/new.js',
    find: `  const options = parseArgs(argv);\n  if (options === null) {\n    process.stderr.write(USAGE);\n    return 2;\n  }\n\n  const selected = selectAdapter(ctx.adapters);`,
    replace: `  const options = parseArgs(argv);\n\n  const selected = selectAdapter(ctx.adapters);`,
    claimedBy: Object.freeze(['test/new.test.mjs']),
    why: 'malformed argv must stop before adapter selection, server resolution, or any state write',
  }),
  Object.freeze({
    id: 'MUT-PIPELINE-DROP-SPAWN-OWNED-FIELDS',
    file: 'src/cli/pipeline.js',
    find: `Object.freeze(['lifecycle', 'flags.parked', 'name', 'state', 'binding']);`,
    replace: `Object.freeze(['lifecycle', 'flags.parked', 'name']);`,
    claimedBy: Object.freeze(['test/pipeline.test.mjs']),
    why: 'reconciliation must not erase spawn-owned state, binding, or their provenance on the next listing',
  }),

  // --- T17 mutants
  Object.freeze({
    id: 'MUT-PARKSTATE-FLAG-ONLY',
    file: 'src/core/parkstate.js',
    find: `    lifecycle,\n    flags: Object.freeze({ ...record.flags, parked: lifecycle === 'Parked' }),`,
    replace: `    flags: Object.freeze({ ...record.flags, parked: lifecycle === 'Parked' }),`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'writing the compatibility flag without the lifecycle field breaks the required derived-field invariant',
  }),
  Object.freeze({
    id: 'MUT-PARK-UNCAUGHT-FSM-ERROR',
    file: 'src/cli/verbs/park.js',
    find: `  let updated;\n  try {\n    updated = applyLifecycle(record, 'park', { at: Date.now() });\n  } catch (error) {\n    if (error instanceof LifecycleVocabularyError) {\n      return refusal(shapedTargetRefusal(record, state, error));\n    }\n    assertInvariant(record);\n    return refusal(\`\${state}: \${error instanceof Error ? error.message : String(error)}\`);\n  }`,
    replace: `  const updated = applyLifecycle(record, 'park', { at: Date.now() });`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'an illegal FSM transition must resolve through the verb refusal contract rather than reject as an uncaught error',
  }),
  Object.freeze({
    id: 'MUT-PARK-UNKNOWN-LIFECYCLE-THROWS',
    file: 'src/cli/verbs/park.js',
    find: `    if (error instanceof LifecycleVocabularyError) {\n      return refusal(shapedTargetRefusal(record, state, error));\n    }`,
    replace: `    if (error instanceof LifecycleVocabularyError) {\n      throw error;\n    }`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'a corrupt stored lifecycle value must resolve to an actionable refusal naming the session, not a stack trace',
  }),
  Object.freeze({
    id: 'MUT-UNPARK-UNCAUGHT-FSM-ERROR',
    file: 'src/cli/verbs/unpark.js',
    find: `  let updated;\n  try {\n    updated = applyLifecycle(record, 'unpark', { at: Date.now() });\n  } catch (error) {\n    if (error instanceof LifecycleVocabularyError) {\n      return refusal(shapedTargetRefusal(record, state, error));\n    }\n    assertInvariant(record);\n    return refusal(\`\${state}: \${error instanceof Error ? error.message : String(error)}\`);\n  }`,
    replace: `  const updated = applyLifecycle(record, 'unpark', { at: Date.now() });`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'an illegal unpark transition must resolve through the verb refusal contract rather than reject as an uncaught error',
  }),
  Object.freeze({
    id: 'MUT-UNPARK-UNKNOWN-LIFECYCLE-THROWS',
    file: 'src/cli/verbs/unpark.js',
    find: `    if (error instanceof LifecycleVocabularyError) {\n      return refusal(shapedTargetRefusal(record, state, error));\n    }`,
    replace: `    if (error instanceof LifecycleVocabularyError) {\n      throw error;\n    }`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'a corrupt stored lifecycle value must resolve to an actionable refusal naming the session, not a stack trace',
  }),
  Object.freeze({
    id: 'MUT-PARK-SWEEPS-UNRELATED-INVARIANTS',
    file: 'src/cli/verbs/park.js',
    find: `  const record = resolved.record;\n  const state = record.lifecycle ?? 'Live';`,
    replace: `  for (const entry of prior.records) assertInvariant(entry.record);\n  const record = resolved.record;\n  const state = record.lifecycle ?? 'Live';`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'an inconsistent unrelated record must not brick a lifecycle operation on a healthy target',
  }),
  Object.freeze({
    id: 'MUT-UNPARK-SWEEPS-UNRELATED-INVARIANTS',
    file: 'src/cli/verbs/unpark.js',
    find: `  const record = resolved.record;\n  const state = record.lifecycle ?? 'Live';`,
    replace: `  for (const entry of prior.records) assertInvariant(entry.record);\n  const record = resolved.record;\n  const state = record.lifecycle ?? 'Live';`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'an inconsistent unrelated record must not brick an unpark operation on a healthy target',
  }),
  Object.freeze({
    id: 'MUT-PARK-SKIPS-TARGET-INVARIANT',
    file: 'src/cli/verbs/park.js',
    find: `  try {\n    assertInvariant(record);\n  } catch (error) {\n    return refusal(shapedTargetRefusal(record, state, error));\n  }`,
    replace: ``,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'a lifecycle transition must reject an inconsistent target rather than silently repair it',
  }),
  Object.freeze({
    id: 'MUT-UNPARK-SKIPS-TARGET-INVARIANT',
    file: 'src/cli/verbs/unpark.js',
    find: `  try {\n    assertInvariant(record);\n  } catch (error) {\n    return refusal(shapedTargetRefusal(record, state, error));\n  }`,
    replace: ``,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'an unpark transition must reject an inconsistent target rather than silently repair it',
  }),
  Object.freeze({
    id: 'MUT-UNPARK-REFUSAL-WRITES',
    file: 'src/cli/verbs/unpark.js',
    find: `    return refusal(\`\${state}: \${error instanceof Error ? error.message : String(error)}\`);`,
    replace: `    await store.writeSession(record.id, { ...record, refusedAt: Date.now() });\n    return refusal(\`\${state}: \${error instanceof Error ? error.message : String(error)}\`);`,
    claimedBy: Object.freeze(['test/park.test.mjs']),
    why: 'a refused lifecycle verb must leave the persisted record byte-identical',
  }),
  Object.freeze({
    id: 'MUT-PIPELINE-DROPS-OWNED-FIELDS',
    file: 'src/cli/pipeline.js',
    find: `    return mergeOwnedFields(reconciled, priorRecord);`,
    replace: `    return reconciled;`,
    claimedBy: Object.freeze(['test/pipeline.test.mjs']),
    why: 'a reconciliation pass must not erase lifecycle fields owned by park and unpark',
  }),

  // --- Lint engine mutants
  Object.freeze({
    id: 'MUT-LINT-NO-SILENT-CATCH-ALWAYS-ACCOUNTED',
    file: 'harness/lint/rules/no-silent-catch.mjs',
    find: `function isAccountedFor(sourceBody, maskedBody) {
  if (commentsMasked(sourceBody) !== sourceBody) return true;
  if (/\\b(?:addCanary|append\\w*Error|notes?\\.push)\\s*\\(/.test(maskedBody)) return true;

  const statement = maskedBody.trim();
  return /^(?:return\\b[^\\r\\n;]*;?|throw\\b[^\\r\\n;]*;?|continue\\s*;?|break\\s*;?)$/.test(statement);
}`,
    replace: `function isAccountedFor() {
  return true;
}`,
    claimedBy: Object.freeze(['test/lint.test.mjs']),
    why: 'treating every bare catch as accounted for lets silent error swallowing pass unnoticed',
  }),
  Object.freeze({
    id: 'MUT-LINT-REFUSAL-THROW-DETECTION-EMPTY',
    file: 'harness/lint/rules/verb-refusals-are-returned.mjs',
    find: `    for (const match of runBody.matchAll(THROW_REFUSAL)) {
`,
    replace: `    for (const match of []) {
`,
    claimedBy: Object.freeze(['test/lint.test.mjs']),
    why: 'skipping direct thrown-refusal matches lets run() reject instead of returning a shaped refusal',
  }),
  Object.freeze({
    id: 'MUT-LINT-DROPS-MUTATING-EXPORT-CHECK',
    file: 'harness/lint/rules/verb-export-contract.mjs',
    find: `    if (!MUTATING_EXPORT.test(masked)) {
`,
    replace: `    if (false) {
`,
    claimedBy: Object.freeze(['test/lint.test.mjs']),
    why: 'dropping the mutating export check lets a verb omit its router safety contract',
  }),
  Object.freeze({
    id: 'MUT-LINT-CONSOLE-DETECTION-EMPTY',
    file: 'harness/lint/rules/no-console.mjs',
    find: `    for (const match of masked.matchAll(CONSOLE_METHOD)) {
`,
    replace: `    for (const match of []) {
`,
    claimedBy: Object.freeze(['test/lint.test.mjs']),
    why: 'skipping console-method matches lets product source write through the forbidden console API',
  }),
  Object.freeze({
    id: 'MUT-LINT-CLI-SUBPROCESS-DETECTION-EMPTY',
    file: 'harness/lint/rules/cli-subprocess-uses-node.mjs',
    find: `    for (const match of masked.matchAll(CURRENT_RUNTIME_LAUNCH)) {
`,
    replace: `    for (const match of []) {
`,
    claimedBy: Object.freeze(['test/lint.test.mjs']),
    why: 'skipping current-runtime launches lets repository executables run under Bun instead of Node',
  }),

  // --- Architecture documentation mutant
  Object.freeze({
    id: 'MUT-ARCHITECTURE-PATH-CHECK',
    file: 'test/architecture-doc.test.mjs',
    find: `  return citedPaths(markdown).filter((relativePath) => !existsSync(path.resolve(root, relativePath)));`,
    replace: `  return citedPaths(markdown).filter((relativePath) => existsSync(path.resolve(root, relativePath)));`,
    claimedBy: Object.freeze(['test/architecture-doc.test.mjs']),
    why: 'the synthetic missing-path control must reject a citation checker whose existence predicate is inverted',
  }),

  // --- Commit convention mutants
  Object.freeze({
    id: 'MUT-COMMIT-DROPS-DERIVED-SCOPES',
    file: 'harness/commitlint.mjs',
    find: `  .filter((entry) => entry.isDirectory())\n`,
    replace: `  .filter(() => false)\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'every current src directory must become an accepted scope without a hardcoded list',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-LOOSE-HEADER',
    file: 'harness/commitlint.mjs',
    find: `const HEADER_PATTERN = /^([a-z]+)\\(([a-z0-9-]+)\\)(!)?: (.*)$/;\n`,
    replace: `const HEADER_PATTERN = /^([a-z]+)\\(([a-z0-9-]+)\\)(!)? ?(.*)$/;\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the colon and following space are mandatory header delimiters',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-MISSING-SCOPE',
    file: 'harness/commitlint.mjs',
    find: `    if (MISSING_SCOPE_PATTERN.test(header)) {\n`,
    replace: `    if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'a missing scope must produce the specific required-scope violation',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-UNKNOWN-TYPE',
    file: 'harness/commitlint.mjs',
    find: `    if (!TYPES.includes(type)) {\n`,
    replace: `    if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'types outside the closed Conventional Commit vocabulary must be rejected',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-UNKNOWN-SCOPE',
    file: 'harness/commitlint.mjs',
    find: `    if (!SCOPE_VOCABULARY.includes(scope)) {\n`,
    replace: `    if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'scopes outside the repository-derived vocabulary must be rejected',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-LONG-HEADER',
    file: 'harness/commitlint.mjs',
    find: `  if (lengthOf(header) > 72) {\n`,
    replace: `  if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'commit headers are capped at 72 characters',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-EMPTY-SUBJECT',
    file: 'harness/commitlint.mjs',
    find: `    if (subject.length === 0) {\n`,
    replace: `    if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'an empty subject must report its own violation',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-CAPITAL-SUBJECT',
    file: 'harness/commitlint.mjs',
    find: `      if (!/^[a-z]/.test(subject)) {\n`,
    replace: `      if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'subjects must begin with a lowercase letter',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-PERIOD',
    file: 'harness/commitlint.mjs',
    find: `      if (subject.endsWith('.')) {\n`,
    replace: `      if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'subjects ending in a period must be refused rather than rewritten',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-NONIMPERATIVE',
    file: 'harness/commitlint.mjs',
    find: `      if (NON_IMPERATIVE_FIRST_WORD.test(subject.split(/\\s+/, 1)[0])) {\n`,
    replace: `      if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the subject must use an imperative verb form',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-LONG-SUBJECT',
    file: 'harness/commitlint.mjs',
    find: `      if (lengthOf(subject) > 72) {\n`,
    replace: `      if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'subjects are independently capped at 72 characters',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-UNSEPARATED-BODY',
    file: 'harness/commitlint.mjs',
    find: `  if (!hasBlankSeparator) {\n`,
    replace: `  if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'a non-empty body must be separated from the header by a blank line',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-ACCEPTS-LONG-BODY-LINE',
    file: 'harness/commitlint.mjs',
    find: `    if (lengthOf(lines[index]) > 100) {\n`,
    replace: `    if (false) {\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'each commit body line is capped at 100 characters',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-KEEPS-GIT-COMMENTS',
    file: 'harness/commitlint.mjs',
    find: `      if (SCISSORS_PATTERN.test(commentBody)) break;\n      continue;\n`,
    replace: `      if (SCISSORS_PATTERN.test(commentBody)) break;\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'git comment lines must be removed before body line-length validation',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-KEEPS-SCISSORS-DIFF',
    file: 'harness/commitlint.mjs',
    find: `      if (SCISSORS_PATTERN.test(commentBody)) break;\n`,
    replace: ``,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'git verbose diff content below the scissors marker is not authored commit text',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-DROPS-LONG-REAL-BODY',
    file: 'harness/commitlint.mjs',
    find: `    authored.push(line);\n`,
    replace: `    if (line.length <= 100) authored.push(line);\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'cleanup must not drop genuine body lines merely because they would fail validation',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-REJECTS-MULTI-CHAR-COMMENTS',
    file: 'harness/commitlint.mjs',
    find: `  return commentChar;\n`,
    replace: `  return [...commentChar].length === 1 ? commentChar : DEFAULT_COMMENT_CHAR;\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'multi-character core.commentChar values must strip exact-prefix cleanup lines',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-HISTORY-USES-SEPARATOR-FORMAT',
    file: 'test/commit-format.test.mjs',
    find: `const HISTORY_LOG_FORMAT = '--format=tformat:%B';\n`,
    replace: `const HISTORY_LOG_FORMAT = '--format=format:%B';\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'history records need tformat terminators so every commit, including the final one, is NUL-delimited',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-HISTORY-FINDS-ONLY-FIRST-OFFENDER',
    file: 'test/commit-format.test.mjs',
    find: `    failures.map(({ index, violations }) => ({\n      index,\n      codes: violations.map((violation) => violation.code),\n    })),`,
    replace: `    [failures.find(({ index }) => index === 0)].map(({ index, violations }) => ({\n      index,\n      codes: violations.map((violation) => violation.code),\n    })),`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the synthetic multi-message history control must assert every offender position, not only the first offender find() can reach',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-INVALID-CLI-EXITS-ZERO',
    file: 'harness/commitlint.mjs',
    find: `  if (result.valid) return 0;\n`,
    replace: `  if (true) return 0;\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the CLI must exit nonzero when the pure validator reports violations',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-DROPS-STDIN',
    file: 'harness/commitlint.mjs',
    find: `    rawMessage =\n      options.messagePath === undefined ? await readStdin() : await readFile(options.messagePath, 'utf8');\n`,
    replace: `    rawMessage = await readFile(options.messagePath, 'utf8');\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the thin CLI accepts a commit message from stdin when no file path is provided',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-HOOK-IGNORES-MISSING-NODE',
    file: '.githooks/commit-msg',
    find: `if ! command -v node >/dev/null 2>&1; then\n`,
    replace: `if false; then\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the hook must fail closed with a clear diagnostic when node is unavailable',
  }),
  Object.freeze({
    id: 'MUT-COMMIT-HOOK-SCANS-EMPTY-MESSAGE',
    file: '.githooks/commit-msg',
    find: `node "$hook_dir/../harness/secret-scan.mjs" "$1" || exit $?\n`,
    replace: `node "$hook_dir/../harness/secret-scan.mjs" /dev/null || exit $?\n`,
    claimedBy: Object.freeze(['test/commit-format.test.mjs']),
    why: 'the hook must scan the message file git supplied rather than empty content',
  }),
  Object.freeze({
    id: 'MUT-PROCEXEC-DROPS-STDIN',
    file: 'src/io/procexec.js',
    find: `      const stdio = input === undefined ? ['ignore', 'pipe', 'pipe'] : ['pipe', 'pipe', 'pipe'];\n      child = spawn(command, args, { cwd, env, stdio });\n      if (input !== undefined) child.stdin.end(input);\n`,
    replace: `      child = spawn(command, args, { cwd, env, stdio: ['ignore', 'pipe', 'pipe'] });\n`,
    claimedBy: Object.freeze(['test/procexec.test.mjs']),
    why: 'explicit input bytes must be written to child stdin and the pipe must close',
  }),
]);
